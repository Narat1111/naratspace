// This script was originally used for comparing products, but since now all Compare buttons link to Telegram,
// the product comparison logic can be removed or kept for other future uses.

const compareResults = document.getElementById('compare-results');

function renderComparison() {
  compareResults.innerHTML = '<p>Comparison feature has been moved to Telegram. Contact us there for more info.</p>';
}

renderComparison();
